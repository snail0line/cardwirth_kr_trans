# 업데이트 내역

앱 상단의 업데이트 배너(🆕 새 버전)가 뜰 때 무엇이 바뀌었는지 여기서 봅니다.
버전은 `VERSION` 파일 하나로 관리하며, 기능 추가 = 가운데 자리(+0.1.0), 고침 = 끝자리(+0.0.1)입니다.
업데이트는 코드 파일만 교체하고 번역 진행상황(`projects/`)·API 키·시나리오는 그대로 둡니다.

## 1.13.1 (2026-08-30)

**열기 가드** — `/api/open`
- `convert_translate` 변환 결과처럼 한 겹 안에 시나리오가 든 폴더(`<이름>_xml`)를 열면, 하위에
  `Summary.xml` 폴더가 하나뿐일 때 자동으로 그 폴더로 내려가 엽니다. 바깥 폴더 이름으로 빈 프로젝트가
  새로 생겨 배치 번역이 든 프로젝트와 어긋나던 문제(원문만 보임)를 막습니다.
- `*_KR.wsn`(번역 결과물)을 열려고 하면 원문 쪽을 찾아 **대신** 엽니다 — ①저장된 원문 프로젝트
  ②옆의 `<이름>.wsn`(모음 폴더 일괄 변환) ③옆의 `<이름>_xml`/`<이름>` 폴더 순. 어디서도 못 찾으면
  경고만 띄우고 그대로 엽니다. 우회해서 열면 알림이 뜨고 경로 입력칸도 실제 열린 쪽으로 바뀝니다.

## 1.13.0 (2026-08-23)

**조사 보정** — `app/josa.py`
- 자동번역 초안에서 용어집 단어 바로 뒤의 조사가 어긋나는 문제(「륜를」→「륜을」)를 초안 생성 때 자동으로 맞춥니다.
  DeepL 용어집·Azure 이름 치환 어느 경로든 적용되며, 용어 끝이 한글이 아닌 것(변수·영문)은 건드리지 않습니다.
- 그 밖의 자리는 자동으로 고치지 않고 **제안**만 합니다. 용어집 한국어 값과 일치하는 이름 뒤는 기본 선택,
  다른 용어의 앞머리일 수 있는 자리는 확인하도록 비워 둡니다. 문서 전체 훑기는 옵션(`josa_all=1`)이고,
  문서 안에서 그 낱말이 조사 없이 혼자 나오거나 서로 다른 조사 둘 이상과 붙어 나오면 제안하지 않습니다.
- **낱말 사전 가드**: `app/dict/korean-surface.txt`(국립국어원 표준국어대사전 표층형 498,733개, CC BY-SA 2.0 KR)로
  「가을」「사이」「마을」처럼 어절 전체가 낱말인 경우를 조사로 오해하지 않게 거릅니다. 자동 교정 경로에도 같은 가드가
  걸립니다. 사전 파일이 없으면 가드만 꺼지고 기능은 동작합니다.
- 받침 판별은 유니코드 계산, 조사 쌍 13종(을/를·은/는·이/가·과/와·으로/로[ㄹ받침 예외]·이라/라·이랑/랑·이나/나·
  이든/든·이며/며·이야/야·아/야·이여/여).

**📋 문제 목록** — `app/issues.py`, `/api/issues`
- 흩어져 있던 검사 결과(흐름 구조·선택지 중복번역·자동번역 복원 실패·메시지창 넘침·용어 불일치·조사 제안)를
  한 패널에서 봅니다. 종류 칩으로 켜고 끄고, 항목을 누르면 그 문장으로 이동합니다. 조사 제안은 [적용]·일괄 적용 가능.
- 기존 개별 API(`/api/mt_failed`, `/api/overflow`, `/api/dup_choices`, `/api/term_check`)는 그대로 둡니다.

**흐름 구조 검사** — `app/flowcheck.py`
- 같은 파일 안에서 `Link/Call` 이 가리키는 `Start` 이름이 없을 때, `Package/Area/Battle` 대상 id 가 없을 때 = 오류.
  어디서도 참조되지 않는 `Start`, 시작 에리어에서 닿지 않는 씬 = 경고. 저장·내보내기를 막지는 않습니다
  (원작 시나리오의 원래 결함일 수 있어서).

**기타**
- README 에 위 기능과 낱말 자료 출처(CC BY-SA) 고지 추가. 낱말 자료(약 6MB) 때문에 자동 업데이트 zip 이 그만큼 커집니다.
- 이 파일(`CHANGELOG.md`)을 자동 업데이트 대상에 포함. 업데이트 배너에 내역 링크.

## 1.12.0 (2026-07-22)
- 🔑 식별자·키코드 번역 패널: 플래그·스텝·쿠폰·고시프·시나리오 이름·키코드를 정의부와 참조부에 동시에 적용해 번역.
  `Link/Start/Call` 라벨도 선택 항목으로 편집 가능(진행률에는 포함하지 않음).
- 키코드 병기: 일본어 키코드 줄을 유지한 채 사전(`app/keycodes.json`)에 맞는 한국어 줄을 덧붙입니다(수동 "병기 채우기" 버튼).

## 1.11.1 (2026-07-05)
- 메시지창 넘침 기준을 8줄 → 7줄로(실제 잘리는 기준).

## 1.11.0 (2026-07-04)
- 넘침 상세 정돈 강화(4단계) + 파일 사이드바 접기.

## 1.10.2 (2026-07-04)
- Ctrl+S 저장 단축키.

## 1.10.1 (2026-07-04)
- 용어 불일치 무시(문장/용어 단위, 해제 가능).

## 1.10.0 (2026-07-04)
- 공용 용어집(전 시나리오 공통, `projects/global_terms.json`) + 기존 번역에 소급 적용 도구 + UI 개선.

## 1.9.1 (2026-07-04)
- Azure 요청 빈도 자체 제한, 용어집 위치 툴팁을 번역문으로.

## 1.9.0 (2026-07-04)
- DeepL 공식 용어집(ja→ko) 연동 + 번역문 찾아 바꾸기 + UI 다듬기.

## 1.8.0 (2026-07-04)
- 원문 초기화(파일/전체) + 동일 원문 수정 일괄 반영.

## 1.7.x (2026-07-04)
- 용어집 확장·툴 전용 이름 번역·흐름 보기 내비/일렬 모드(1.7.0), 용어집 편의·2단 헤더·자동번역 복원 실패 가시화(1.7.2).

## 1.6.0 (2026-07-04)
- README 번역 대상 분류 갱신.

## 1.5.0 (2026-07-04)
- Azure Translator 보조 엔진(이름 치환 방식) + 사용량 자체 집계.

## 1.1.0 (2026-07-01)
- 정돈(줄바꿈 정리), DeepL 후처리(들여쓰기·말줄임표·따옴표), 선택지 중복번역 감지, 넘침 목록, 저장 다이얼로그, 용어 정리 배치.
